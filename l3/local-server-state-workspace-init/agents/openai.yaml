interface:
  display_name: "Local State Workspace Init"
  short_description: "Atomic initialization of local server-state markdown workspace"
  default_prompt: "Use $local-server-state-workspace-init to create the local server-state workspace skeleton and default markdown files."
