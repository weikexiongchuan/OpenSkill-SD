# Template Positioning

## Rule
- 不再用含糊的“稳妥”做模板标签，统一改用更可执行的定位词。

## Positioning
- `保守起手`：优先可控、好复盘，不追求第一版就学满。
- `标准推荐`：效果、成本、容错之间的默认平衡档。
- `激进吸收`：优先快速学进去，接受更高的过拟合和算力压力。
- `触发词优先`：优先角色名、身份词和触发词稳定。

## Read Rule
- 场景层默认先给 `保守起手`，再给 `标准推荐`。
- 只有用户明确要更强吸收，才切到 `激进吸收`。
- 只有用户明确要更稳的角色词绑定，才强调 `触发词优先`。
